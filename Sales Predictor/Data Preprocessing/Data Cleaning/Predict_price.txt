# Load the model
import joblib

model = joblib.load('model_1')

data = pd.read_csv('encoded_sales_data.csv')
data = pd.DataFrame(data)

# Specify the name of the target column
target_column = 'Item_Outlet_Sales'  # replace with your actual target column name

selected_row = data.iloc[[2]]  # Change the index to select the correct row

# Remove the target column from selected_row before prediction
selected_row = selected_row.drop(columns=[target_column])


# Make the prediction
predicted_price = model.predict(selected_row)

# Print the predicted price
print(f"The predicted price of the product is: {predicted_price[0]}")